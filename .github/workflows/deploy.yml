name: Deploy to VPS

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - run: npm ci
      
      - name: Build with environment variables
        run: npm run build
        env:
          MONGODB_URI: ${{ secrets.MONGODB_URI }}

      - name: Deploy
        uses: easingthemes/ssh-deploy@main
        with:
          SSH_PRIVATE_KEY: ${{ secrets.SSH_KEY }}
          REMOTE_HOST: ${{ secrets.HOST }}
          REMOTE_USER: ${{ secrets.USER }}
          TARGET: /var/www/rnadw
          EXCLUDE: ".git, node_modules, .env.local"

      - name: Setup environment and restart PM2
        uses: appleboy/ssh-action@v1.2.0
        with:
          host: ${{ secrets.HOST }}
          username: ${{ secrets.USER }}
          key: ${{ secrets.SSH_KEY }}
          command_timeout: 10m
          script_stop: true
          script: |
            source ~/.bashrc || true
            source ~/.nvm/nvm.sh || true
            cd /var/www/rnadw
            echo "MONGODB_URI=${{ secrets.MONGODB_URI }}" > .env.local
            mkdir -p /var/www/rnadw/public/uploads
            npm ci --prefer-offline
            pm2 restart rnadw || pm2 start npm --name rnadw -- start
